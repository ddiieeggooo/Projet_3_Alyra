/*import { Spinner, Text } from "@chakra-ui/react"

import { formatEther } from "viem"

const GetVoter = ({ isPending, balance }) => {
    return (
        <>
            {isPending ? (
                <Spinner />
            ) : (
                <Text>This Voter: <Text as='b'>{addressOfVoter} ETH</Text></Text>
            )}
        </>
    )
}

export default GetVoter

*/
